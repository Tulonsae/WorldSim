In progress - turn this into real documentation.

These functions do not check if the Id passed to them is unique or already
used.  It is up to the calling program to handle whether the Id is correct.
However, these do check if the Id is valid (i.e., not negative or zero).
